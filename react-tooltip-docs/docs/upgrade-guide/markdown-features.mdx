---
sidebar_position: 4
---

import { Tooltip } from 'react-tooltip'
import 'react-tooltip/dist/react-tooltip.css'

# Basic example

## MDX and React Components

[MDX](https://mdxjs.com/) can make your documentation more **interactive** and allows using any **React components inside Markdown**:

```jsx
const TooltipAnchor = ({ children }) => {
  return (
    <span
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        width: '60px',
        height: '60px',
        borderRadius: '60px',
        color: '#222',
        background: 'rgba(255, 255, 255, 1)',
        cursor: 'pointer',
        boxShadow: '3px 4px 3px rgba(0, 0, 0, 0.5)',
        border: '1px solid #333',
      }}
      onClick={() => {
        alert('clicked on Tooltip Anchor')
      }}
    >
      {children}
    </span>
  )
}

;<TooltipAnchor>◕‿‿◕</TooltipAnchor>
;<Tooltip anchorId="tooltip-anchor" content="hello world!" />
```

export const TooltipAnchor = ({ children, id }) => {
  return (
    <span
      id={id}
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        width: '60px',
        height: '60px',
        borderRadius: '60px',
        color: '#222',
        background: 'rgba(255, 255, 255, 1)',
        cursor: 'pointer',
        boxShadow: '3px 4px 3px rgba(0, 0, 0, 0.5)',
        border: '1px solid #333',
      }}
      onClick={() => {
        alert('clicked on Tooltip Anchor')
      }}
    >
      {children}
    </span>
  )
}

<TooltipAnchor id="tooltip-anchor">◕‿‿◕</TooltipAnchor>
<Tooltip anchorId="tooltip-anchor" content="hello world!" />
